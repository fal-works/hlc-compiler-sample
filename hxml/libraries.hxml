# Define libraries to be included.
# This file is referenced from other hxml files.

# ---- Heaps.io -----------------------------------
--library heaps

# ---- SDL or DirectX (select either one) ---------
--library hlsdl
# --library hldx

# ---- For including/excluding other libraries ----
--define USE_UV
--define USE_SSL
--define USE_MYSQL
